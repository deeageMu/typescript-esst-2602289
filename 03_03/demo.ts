export function Demo() {
  let DARK_MODE: false = false;

  const isDarkMode: boolean = DARK_MODE;

  let errorCode: "ERROR_404" | "ERROR_500" | "ERROR_503";

  errorCode = "ERROR_500";


  const numbersOrStringsArray: (number | string)[] = [1, 'hello']
  const numbersOrStringsMultiArray: (number[] | (string | number)[])[] = [[1], ['hi', 1]]

}
